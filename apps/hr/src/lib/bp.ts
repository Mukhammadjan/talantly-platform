/** basePath (/panel) — client fetch va window.location uchun prefiks.
 *  next/link va router avtomatik qo'shadi, qo'lda URL'lar esa BP orqali. */
export const BP = process.env.NEXT_PUBLIC_BASE_PATH ?? "";
